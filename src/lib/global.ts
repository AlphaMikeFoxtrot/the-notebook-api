const global: any = {
    firebase: {
        firestore: {
            collections: {
                courses: "courses",
                departments: "departments",
                documents: "documents"
            }
        }
    }
};

export default global;
