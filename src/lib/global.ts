const global: any = {
    firebase: {
        firestore: {
            collections: {
                courses: "courses",
                departments: "departments",
                documents: "documents",
                subjects: "subjects"
            }
        }
    }
};

export default global;
