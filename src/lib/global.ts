const global: any = {
    firebase: {
        firestore: {
            collections: {
                courses: "courses",
                departments: "departments",
                documents: "documents",
                subjects: "subjects",
                users: "users"
            }
        }
    },
    hash: {
        salts: 12
    }
};

export default global;
